import React from 'react'
import { styles } from '../styles'

const Slider= () => {
  return (
    <section  className={`${styles.paddingX} bg-tertiary w-full mx-auto flex flex-row justify-center items-start gap-5 p-20`}>
      <div>
        <h1>Image Slider</h1>
      </div>
    </section>
  )
}

export default Slider